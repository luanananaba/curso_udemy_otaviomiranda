Boolean data type.
bool

When querying something in a program, there are only two possible answers:
yes (True) or no (False).

There are several operators for "querying".

Among them is ==, which is a logical operator that queries if one value is equal to another.